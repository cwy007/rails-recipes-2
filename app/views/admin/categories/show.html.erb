<h1><%= @category.name %> - Events</h1>

<table class="table table-bordered">
  <tr>
    <th>#</th>
    <th>Name</th>
    <th>Status</th>
    <th>Created_at</th>
  </tr>
  <% @events.each do |event| %>
  <tr>
    <td><%= event.friend_id %></td>
    <td><%= link_to( event.name, event_path(event) ) %></td>
    <td><%= event.status %></td>
    <td><%= event.cteated_at.to_s %></td>
  </tr>
  <% end %>
</table>
